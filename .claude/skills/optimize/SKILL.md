---
name: optimize
description: Diagnose and fix UI performance across loading speed, rendering, animations, images, and bundle size. Use when the user mentions slow, laggy, janky, performance, bundle size, load time, or wants a faster, smoother experience.
metadata:
  argument-hint: "[target]"
---

This compatibility wrapper exposes the `optimize` skill for the `.claude/skills` layout while keeping `skills/optimize/SKILL.md` as the canonical source of truth.

Follow the canonical instructions in [../../../skills/optimize/SKILL.md](../../../skills/optimize/SKILL.md).
